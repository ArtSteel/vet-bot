# ВетСоветник AI — Подробное описание проекта

## О проекте

**ВетСоветник AI** — Telegram-бот, карманный ветеринарный ассистент на базе ИИ. Цель: помогать владельцам питомцев разбираться в здоровье, анализах и уходе, переводить медицинские термины на понятный язык и давать первую ориентировку (без замены очного визита к врачу).

Бот написан на **Python 3**, использует **aiogram 3.x**, асинхронное хранилище (**SQLAlchemy 2.0 Async**) и ИИ через **vsegpt.ru** (OpenAI-совместимый API). Поддерживаются **SQLite** и **PostgreSQL**.

---

## Основные возможности

### 1. Консультации 24/7
- Ответы на вопросы о здоровье, питании и уходе за питомцами.
- Учёт контекста диалога (последние сообщения из истории).
- Разные лимиты и модели по тарифам: FREE / PLUS / PRO.

### 2. Расшифровка анализов (фото/документы)
- Загрузка фото результатов (кровь, УЗИ и т.п.) или PDF.
- Анализ по системе «Светофор»: 🔴 критично, 🟡 внимание, 🟢 в норме.
- Первая расшифровка — бесплатно (пробный анализ).
- Дальше — по тарифам или разовым покупкам (баланс анализов).

### 3. Медкарта питомца
- Несколько питомцев на пользователя (собака/кошка).
- Поля: кличка, порода, возраст, вес, хронические болезни и др.
- Используется для более точных советов и расчёта дозировок.
- Команда `/medcard` и кнопка «🐕 Медкарта».

### 4. Монетизация
- **FREE** — ограниченное число текстовых сообщений и фото в день/месяц.
- **Разовый разбор** — 99₽, +1 расшифровка в баланс, не сгорает.
- **Подписка PLUS** — 299₽/мес: больше сообщений, до 10 фото/документов в месяц.
- **Подписка PRO** — 590₽/мес: всё из PLUS, до 20 фото/документов в месяц.
- Оплата через **YooKassa** (polling без вебхуков).

### 5. Промокоды и реферальная программа
- Промокоды типов: `subscription_days` (дни подписки) и `balance_add` (анализы).
- Реферальные ссылки: `?start=ref_USER_ID` — бонус +1 анализ пригласившему и новому пользователю.
- Активация промокода: `/promo CODE` или кнопка «🎟 Промокод» (FSM).
- Админ: создание промокодов через админ-панель (FSM).

### 6. Админ-панель
- Команда `/admin` (доступ по `ADMIN_IDS`).
- Кнопки: Общая статистика, Финансы, Создать промокод, Рассылка, Поиск юзера, Выйти.
- Финансы: выручка за сегодня/всего, средний чек, количество транзакций.
- Рассылка: текст или фото с подписью, превью, подтверждение, отправка всем пользователям.
- Все хендлеры админки проверяются на `is_admin()`.

### 7. Логирование действий пользователей
- Middleware логирует каждое действие с тегом тарифа: `[💎 SUB]`, `[💰 1-TIME]`, `[🆓 FREE]`.
- Формат: `👤 [ID:{user_id} | {TAG}] -> {Action}` (текст обрезается, callback_data обрезается).
- Стандартные логи aiogram отключены (уровень WARNING), чтобы не засорять консоль.

---

## Структура проекта

```
Vet-bot/
├── bot.py                 # Точка входа, диспетчер, единая точка входа в ИИ (unified_ai_entry)
├── ai_client.py           # Клиент VseGPT (OpenAI-compatible chat/vision)
├── storage.py             # Вся работа с БД: пользователи, питомцы, лимиты, платежи, промокоды
├── models.py              # ORM: User, Pet, History, YooKassaPayment, Feedback, PromoCode, PromoUsage
├── requirements.txt       # Зависимости
├── .env / .env.example    # Конфигурация (токены, лимиты, БД)
├── handlers/
│   ├── core.py            # /start, /me, помощь, кнопки (Медкарта, Подписка, Бонусы, Что умеет бот)
│   ├── medcard.py         # Медкарта: создание/выбор/редактирование питомцев, FSM ввода полей
│   ├── ocr.py             # Обработка фото/документов, вызов vision-модели
│   ├── pay.py             # YooKassa: создание платежей, polling успешных оплат, активация подписок
│   ├── promo.py           # /promo, кнопка «Промокод», FSM ввода кода
│   ├── admin.py           # /admin, статистика, финансы, промокоды, рассылка, поиск юзера
│   ├── feedback.py        # Сбор 👍/👎 по ответам
│   ├── menu.py            # Inline-меню (помощь, медкарта, подписка)
│   └── states.py          # FSM: PromoState, AdminPromoState, AdminBroadcastState, AdminSearchState
├── keyboards/
│   ├── main_kb.py         # Reply-клавиатура (Задать вопрос, Анализ фото, Медкарта, Подписка, Бонусы, Промокод, Что умеет бот)
│   └── admin_kb.py        # Админ-клавиатура
├── middlewares/
│   └── logger_middleware.py  # LoggingMiddleware: тег тарифа + действие пользователя
├── deploy/                # systemd-сервисы для VPS
├── .github/workflows/     # CI/CD: деплой на VPS по push в main
└── migrate_db.py, POSTGRES_MIGRATION.md  # Миграция данных SQLite → PostgreSQL
```

---

## Технологический стек

| Компонент        | Технология |
|------------------|------------|
| Язык             | Python 3 |
| Telegram API     | aiogram 3.15 |
| БД               | SQLAlchemy 2.0 (async), aiosqlite / asyncpg |
| ИИ               | VseGPT (vsegpt.ru), OpenAI-compatible chat/completions + vision |
| Оплата           | YooKassa (REST API, polling) |
| Конфиг           | python-dotenv, .env |
| Деплой           | systemd, GitHub Actions (SSH + git pull + restart) |

---

## Модели данных (кратко)

- **User** — telegram user_id, username, status/tier, лимиты (daily_usage, photos_month), подписка (sub_end_date), balance_analyses, referrer_id и др.
- **Pet** — привязка к user_id, имя, тип (dog/cat), порода, возраст, вес, хронические болезни и т.д.
- **History** — история сообщений (user_id, user_text, bot_text, created_at) для контекста диалога.
- **YooKassaPayment** — payment_id, user_id, tier, created_at (защита от двойной активации).
- **Feedback** — user_id, entry_id, kind (like/dislike), source.
- **PromoCode** — code, type, value, max_uses, current_uses, expiry_date.
- **PromoUsage** — user_id, promo_code_id, used_at.

---

## Лимиты и тарифы (настраиваются через .env)

- **FREE**: дневной лимит текстовых сообщений (`FREE_DAILY_TEXT_LIMIT`), лимит фото в месяц (`FREE_PHOTOS_PER_MONTH`), первая расшифровка бесплатно (trial).
- **PLUS/PRO**: свои лимиты сообщений и фото, более мощные модели (например, Qwen3-Max для текста, vision для фото).
- Подписка и разовая покупка дают безлимит по тексту на период/24 часа соответственно; фото считаются по месячному лимиту тарифа или списываются с баланса анализов.

---

## Безопасность и ограничения

- Админ-панель и админ-команды доступны только пользователям из `ADMIN_IDS`.
- Все хендлеры в `admin.py` проверяют `is_admin(user_id)`.
- В ответах бота добавляется единый юридический дисклеймер (ИИ не заменяет врача в экстренных случаях); дубли и «страшные» формулировки в ответах модели отфильтровываются в коде.

---

## Деплой

- **Локально**: `python -m venv .venv`, `pip install -r requirements.txt`, создать `.env`, запуск `python bot.py`.
- **VPS**: копирование проекта в `/opt/vet-bot`, venv, установка systemd-сервиса из `deploy/`, при необходимости — настройка PostgreSQL и переменных `POSTGRES_*` / `DATABASE_URL`.
- **Автодеплой**: при `git push` в `main` workflow подключается по SSH к VPS, выполняет `git pull`, обновление зависимостей и перезапуск сервиса. Подробности — в `README.md` и `DEPLOY_INSTRUCTIONS.md`.

---

## Цель продукта

Дать владельцам питомцев быстрый и понятный первый уровень помощи: расшифровка анализов, ответы на типовые вопросы, учёт данных питомца — с чётким разделением по тарифам и без замены очного визита к ветеринару в серьёзных случаях.
