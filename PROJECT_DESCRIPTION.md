# VetAdvice AI (ВетСоветник) — описание проекта

## О проекте

**VetAdvice AI / ВетСоветник** — Telegram-бот, карманный AI‑ассистент для владельцев домашних животных. Он помогает:
- быстро понять смысл анализов и симптомов,
- перевести “медицинский язык” на понятный,
- подсветить потенциальные риски и срочность.

Проект написан на **Python**, использует **aiogram 3.x**, **PostgreSQL (SQLAlchemy Async)**, **Redis** для FSM‑состояний и контейнеризацию **Docker + Docker Compose**.  
AI‑интеграция реализована через **OpenAI-compatible API** (по умолчанию — `vsegpt.ru`, но `AI_BASE_URL` можно заменить на любой совместимый endpoint).

---

## Ключевые возможности

### 1) Консультации 24/7 (текст)
- Ответы на вопросы о здоровье, уходе и питании.
- Контекст диалога: бот хранит историю и использует её при генерации.
- Лимиты на текстовые сообщения настраиваются через `.env`.

### 2) Расшифровка анализов (фото/PDF) и “Светофор”
- Принимает фото и PDF (vision/OCR).
- Выдаёт разбор по системе “Светофор”:
  - 🔴 критично,
  - 🟡 обратить внимание,
  - 🟢 в норме.
- Встроен **trial**: первая расшифровка может быть бесплатной (флаг `is_trial_used`), далее — по подписке или разовым покупкам.

### 3) Медкарта (мульти‑питомец)
Профиль питомца (вид, возраст, вес, хроника и др.) повышает точность ответов и помогает давать советы с учётом контекста.

### 4) Монетизация (опционально)
- Разовый разбор (баланс анализов)
- Подписки (PLUS/PRO)
- Интеграция с YooKassa (polling)
- Промокоды и реферальная система

### 5) Админ-панель
Команда `/admin` (доступ по `ADMIN_IDS`):
- статистика,
- финансы,
- создание промокодов (FSM),
- рассылка (FSM),
- поиск пользователя.

### 6) Логирование действий пользователей
Middleware пишет “красивые” логи действий: ID пользователя + тег тарифа + действие (текст/кнопка/фото).

---

## Структура репозитория

```
vet-bot/
├── bot.py                 # Точка входа, диспетчер, unified_ai_entry
├── config.py              # Централизованная конфигурация (env)
├── check_env.py           # Smoke-check критических env-переменных
├── ai_client.py           # OpenAI-compatible клиент (base_url настраивается)
├── storage.py             # Async SQLAlchemy storage (PostgreSQL приоритетно)
├── models.py              # ORM модели
├── handlers/              # Хендлеры aiogram (core, ocr, pay, promo, admin, ...)
├── keyboards/             # Reply/Inline клавиатуры
├── middlewares/           # Middleware (логирование действий и т.д.)
├── docker-compose.yml     # app + db + redis
├── Dockerfile             # Образ бота
├── .dockerignore          # Исключения для docker build context
├── requirements.txt       # Python зависимости
├── .env.example           # Пример конфигурации
├── POSTGRES_MIGRATION.md  # Инструкция миграции SQLite → PostgreSQL
└── LICENSE                # MIT
```

---

## Технологический стек

| Компонент | Технология |
|---|---|
| Язык | Python 3 |
| Telegram | aiogram 3.x |
| База данных | PostgreSQL + SQLAlchemy 2.0 Async (`asyncpg`) |
| FSM storage | Redis |
| OCR/Vision | OpenAI-compatible endpoint (через `AI_BASE_URL`) |
| Контейнеризация | Docker + Docker Compose |
| Оплата (опционально) | YooKassa (polling) |

---

## Конфигурация (.env)

Критические переменные (проверяются `check_env.py` при старте):
- `TELEGRAM_BOT_TOKEN`
- `AI_API_KEY`
- `REDIS_URL`

Для базы PostgreSQL (используется **приоритетно**, если заданы `POSTGRES_*`):
- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `POSTGRES_HOST`, `POSTGRES_PORT`

Все параметры перечислены в `.env.example`.

---

## Важный медицинский дисклеймер

**VetAdvice AI не является медицинской услугой и не заменяет очный приём у лицензированного ветеринарного врача.**  
ИИ может ошибаться, упускать важные детали или давать небезопасные рекомендации.  
При острых, тяжёлых или быстро ухудшающихся симптомах немедленно обращайтесь в ветеринарную клинику.

